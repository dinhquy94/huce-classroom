import React from 'react';
import ContentView from '../../src/layouts/auth/login';
import Keychain from 'react-native-keychain'


export const SignIn2Screen = (props): React.ReactElement => {
  
  
   

  return ( <ContentView navigation={props.navigation}/>)
}
 
 
