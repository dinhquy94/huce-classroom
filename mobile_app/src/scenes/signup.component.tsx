import React from 'react';
import ContentView from '../../src/layouts/auth/sign-up';

export const SignUpScreen = ({ route, navigation }): React.ReactElement => (
  <ContentView navigation={route, navigation}/>
);
